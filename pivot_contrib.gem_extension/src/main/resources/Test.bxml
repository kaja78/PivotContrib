<!DOCTYPE BXML>
<Frame xmlns:bxml="http://pivot.apache.org/bxml" xmlns="org.apache.pivot.wtk"
	   xmlns:view="com.gem.bambiConsole.mainWindow" xmlns:content="org.apache.pivot.wtk.content"
	   xmlns:gem="com.gem.pivot.wtk.generated_form"
	   title="${model.appTitle}" maximized="true"
	   styles="{ padding: { top:0, left:4,	bottom:4, right:4 }, showWindowControls:false }"
	   bxml:id="mainWindowFrame">

	<gem:AutoGeneratedFormComponent
			dynFormPerform="${controller}"
			someInst="${model.testForm}"
			performButton="Send Form"
			clearButton="Clear Form"/>
</Frame>
